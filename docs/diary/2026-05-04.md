# 2026-05-04 — F2.7 Sezení 12

## Sezení 12 — PoseSequence refactor

### Diskuse

- **@BEGIN.** Git up-to-date, server na 8000 spuštěn. Shrnutí stavu z Sezení 11: Demo03 (linkTo s 5 fázemi) + Demo04 (Chair Stage 1 + Bed Stage 2) hotové; F2.6 Stage 3 (PoseSequence refactor) připraven jako první bod programu (= 2. klient existuje, premature abstraction warning teď neplatí). Návrh: začít refaktorem, alternativa rotation order fix odložena (= jen workaround úklid, ne strategický). Uživatel: A.

- **Mapování use cases.** Před návrhem API jsem prošel oba klienty:
  - demo03 anchor A/B/C + click: `linkTo({x,z[,then]})`
  - demo04 židle: `linkTo({x,z,finalYaw,arrivePosition,then})`
  - demo04 postel: `linkTo(...)` + externí `bedStep` state machine v render loopu
  - 4 nálezy: (1) postel je jediný real driver pro `transitionTo` — bez něj je extrakce čistá kosmetika; (2) leaky abstraction (`isStickmanIdle()` v demu sahá na 3 internals, Reset polohy přepisuje 7 fields); (3) `linkTo` je už dneska mini-sekvence, PoseSequence ji jen explicitně rozkrojí; (4) memory `feedback_skeleton_refactor_checklist` se týká Skeleton, tady se kostry nedotýkáme.

- **3 varianty API + doporučení hybrid C.** Builder fluent (z IDEAS.md) vs. step array data-driven vs. hybrid (builder venku, array uvnitř). **Doporučil hybrid:** demo kód čte líp v builderu, Brain dostane array form pod kapotou. **Plus:** odstranit `stickman.linkTo()` úplně (= jediná cesta = SLAP) + `transitionTo({worldPos, worldYaw, status, duration})` jako nový primitive testovaný na posteli sit→lay.

  Memory `feedback_kiss_refactor` říká „modul + 1 vzorové demo, zbytek nech", ale to platí když existuje 5+ klientů; tady jsou jen 2 a oba jsou skutečnými klienty.

  Uživatel: A1-3 souhlas.

- **Implementace.** Postupně:
  1. `src/character/PoseSequence.js` (~340 LOC) — nová třída.
  2. `src/character/Stickman.js` cleanup (478 → 245 LOC).
  3. demo03 přepis.
  4. demo04 přepis (vč. plynulý sit→lay přes `transitionTo`).

- **Edge case fix v `tick()`.** Bez smyčky by chained instant steps (`arrival → setStatus(SIT)`) měly 2-frame delay (~33ms při 60fps) → vidět jako „postava chvíli šlape na místě před sednutím". Smyčka s safety counter (16) zpracuje další step v ten samý frame s `dt = 0`. Steps, které sčítají čas (wait, transitionTo, linkTo moving), break loop a čekají další frame.

- **Plynulý sit→lay v posteli.** Hlavní novinka. `transitionTo({worldPos: layCenter, worldYaw: bed.facing, status: LAY, duration: 0.6})` lerpuje pelvis z sitEdge na layCenter synchronně s SIT→LAY pose blendem. Pose transition má vlastní `stickman.transitionDuration = 0.4 s`, takže pose se přepne dřív než dojede pos lerp (= postava si „lehne na místě, pak doplaže pelvisem na střed"). `Stickman.animate` zapisuje `worldPos.x/z + worldYaw` na konec každého frame, což přepíše blended `rootPosition` z Pose.lerp — fungují vedle sebe bez konfliktu.

- **Test.** Uživatel ověřil v prohlížeči obě dema. Kudos! pro plynulý sit→lay v posteli.

### Rozhodnutí

- **Hybrid API** (builder venku, step array uvnitř). Builder pro demo čitelnost, array pro F3 Brain programatické skládání.
- **`stickman.linkTo()` úplně odstraněno.** Jediná cesta přes `PoseSequence` = SLAP.
- **`transitionTo({worldPos, worldYaw, status, params, duration})`** jako nový primitive. Rozhodl, že pose transition má vlastní `stickman.transitionDuration` (0.4 s default) a pos lerp má `step.duration` — záměrně oddělené (postava si „lehne na místě, pak doplaže").
- **`_computeFootDz` → public `computeBodyForwardSpeed`.** Foot tracking state (`_support`) zůstává na Stickmanovi (= per-instance, invalidate při setStatus). PoseSequence ho volá jako external API. Demo02 si drží vlastní inline implementaci (= 1 klient, ne 2 → premature abstraction memory drží).
- **`tick()` smyčka s safety counter.** Eliminuje 2-frame delay mezi chained steps. Safety = 16 (v praxi max ~4 chained steps).
- **Memory checklist 6 vrstev neaplikovala** — se týká Skeleton refaktoru. Tady se kostry nedotýkáme. Vlastní mini-checklist (Stickman + PoseSequence + 2 dema + 5 docs).
- **`onBed` flag v demu04** zůstává jako demo-specific (= geometrický test by byl drahý + vyžadoval znalost rozměrů postele v render loopu). Snap-to-floor override jen pro `Status.LAY` (ne SIT — pelvis při SIT je sám geometricky na výšce sedátka).

### Kód

#### Nové soubory

- `src/character/PoseSequence.js` (~340 LOC) — nová třída. Builder API + step array. 4 primitives: `linkTo`, `setStatus`, `wait`, `transitionTo`. `run()`, `abort()`, `isDone()`. Tick loop s safety counter.
- `docs/diary/2026-05-04.md` — tento záznam.

#### Upravené (nejdůležitější)

- `src/character/Stickman.js` — cleanup z 478 → 245 LOC. Odstraněno: `linkTo`, `_pickLocomotion`, 11 link fields (`target`, `turning`, `turnFrom`, `turnTo`, `turnElapsed`, `turnDuration`, `_pendingAction`, `_onArrival`, `_finalYaw`, `_arrivePosition`, `_arriveLerp`), ~280 LOC link logiky v `animate()`. Zachováno: `worldPos`, `worldYaw` (zdroj pravdy), foot tracking state. Přidáno: `_activeSeq` field + tick hookup, `isMoving` getter, `_computeFootDz` → public `computeBodyForwardSpeed`.
- `demos/demo03_linker.html` — `goTo(x, z, arriveStatus)` přes `new PoseSequence(s).linkTo({x,z}).setStatus(arriveStatus).run()`. Reset polohy: `seq.abort()` místo přepisování fields. `stickman.isMoving` místo poking `target`/`turning`. Anchor schema `{ ..., arriveStatus: Status.SIT }` místo `{ ..., then: { status: SIT, params: {} } }`.
- `demos/demo04_interactables.html` — externí `bedStep` scheduler smazán. Postel = jedna builder sekvence. Plynulý sit→lay přes `transitionTo`. `onBed` flag pro snap-to-floor override.
- `TODO.md` — F2.5 duplikát smazán; F2.4 duplikát smazán; F2.6 Stage 3 → DONE; nová sekce F2.7 Sezení 12 — HOTOVO; F2.8 Rotation order fix nově oddělená.
- `DONE.md`, `DIARY.md` — záznam Sezení 12.
- `GLOSSARY.md` — sekce „Pose linking (linkTo)" přepsána na „PoseSequence" se 4 primitives.

### Kudos / Censure

- **Kudos!** — uživatel po vizuálním testu obou dem. Plynulý sit→lay v posteli funguje.
- **Žádný Censure!** — návrh A1-3 přijat bez korekce. Testování bezproblémové.
- **Sebehodnocení:** smyčka v `tick()` byla preempivní fix (= predikoval jsem 2-frame delay, ale uživatel ho vizuálně neviděl), ale ne over-engineering — frame-delay je deterministický vedlejší efekt step machinérie, řeší ho 1 řádek loop wrapper. Akceptabilní kompromis za nepatrnou komplexitu.

### Příště

1. **F2.4 Plížení polish.** Otevřené z S10 — paže (shoulder.x ~170° pro body rotation -87°), spine C-curve, foot tracking sign flip pro plížení.
2. **F2.8 Rotation order fix.** Změna `Skeleton.computeWorldTransforms` na `T·Rz·Ry·Rx`. Audit pose, Drift, Inspector, dema 01-04. Po fixu odstranit `bed.facing = 0` workaround. Side-quest.
3. **SWIM, CLIMB, SLEEP statusy.** Dokončení F2 vocabulary; pak smazat demo11 + demo12.
4. **F3 Akvárium.** Multi-instance Stickman + Brain (Markov scheduler nad PoseSequence). PoseSequence je teď připravená — Brain bude generovat step arrays.
