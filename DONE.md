# DONE — Stickman

Hotové úkoly z `TODO.md`. Detaily v příslušných diary záznamech.

## 2026-05-04

- **F2.7 Sezení 12: PoseSequence refactor.** Extrakce link logiky ze Stickmana do skladatelné třídy. Hybrid API (builder venku, step array uvnitř — F3 Brain může skládat sekvence programaticky). 4 primitives: `linkTo`, `setStatus`, `wait`, `transitionTo`. **Nový `transitionTo`** = plynulý lerp pos+yaw paralelně se setStatus pose blendem; nahrazuje teleport v posteli sit→lay. `run()` připojí seq jako `stickman._activeSeq` a abortuje předchozí. `tick(dt)` má smyčku — chained instant steps (arrival → setStatus(SIT)) proběhnou v 1 frame, jinak 2-frame delay viditelný jako „šlape na místě před sednutím". **Stickman cleanup** z 478 → 245 LOC: odstraněny `linkTo`, `_pickLocomotion`, 11 link fields, ~280 LOC link logiky v animate(); zachovány `worldPos`/`worldYaw` jako zdroj pravdy world coords (demo02 treadmill je dál mutuje); `_computeFootDz` → public `computeBodyForwardSpeed`; přidán `_activeSeq` hookup + `isMoving` getter. **demo03_linker přepsán** — `goTo(x, z, arriveStatus)` přes builder, Reset polohy 5 řádků místo 11. **demo04_interactables přepsán** — externí `bedStep` scheduler smazán; postel = jedna builder sekvence: `linkTo → setStatus(SIT) → wait(0.5) → transitionTo({worldPos: layCenter, status: LAY, duration: 0.6})`. Plynulý pelvis lerp z okraje na střed synchronně se SIT→LAY pose blendem. **GLOSSARY.md** — sekce „Pose linking (linkTo)" přepsána na „PoseSequence" se 4 primitives. **Memory `feedback_skeleton_refactor_checklist`** se týkala Skeleton refaktoru (kostra), tady se kostry nedotýkáme — vlastní mini-checklist (Stickman + PoseSequence + 2 dema + GLOSSARY/TODO/DONE/IDEAS/CLAUDE). Detaily v Sezení 12.

## 2026-05-01

- **F2.5 Sezení 11: Demo03 Pose linker (5 fází) + Demo04 Interakce s objekty (židle + postel).** **Demo03** — pose linking prototyp ve štaflových fázích. **Stage 1**: `Stickman.linkTo({x, z})` + `worldPos` field, demo03_linker.html s click→walk; konstantní rychlost 1.5 j/s; bez turn-to (vědomá visual bug). **Stage 2**: turn-to phase přes `worldYaw` field + shortest-path normalize do (-180°, 180°]; postava se otočí k cíli za 0.4 s, pak teprve WALK. Stickman zapisuje worldYaw do `skel.rootRotation.y` na konci animate (= analogicky s worldPos.x/z přes reset z fn()). **Stage 2.5 (= rychlostní fix)**: konstanta `linkSpeed` smazána; rychlost se počítá foot trackingem (= identický algoritmus jako treadmill v demo02 — nejdelší consecutive support history → body Z delta). Helper `_computeFootDz()`. Reset support state v každém setStatus. **Stage 3**: locomotion picker `_pickLocomotion(d)` — `<5j` WALK, `<15j` Jog, `≥15j` Sprint. `_pendingAction` field předá vybraný status z linkTo do post-turn-to dispatch. **Stage 4**: 3 anchors A/B/C (žluté válce + sprite labely přes CanvasTexture s strokem) + tlačítka v levém panelu + ad-hoc click do podlahy + tlačítko Reset polohy. **Stage 5**: cílová pose po dorazu — `linkTo({ x, z, then: { status, params } })` + `_onArrival` field. Anchors B → Sit, C → Lay. **Stage 6 (odloženo)**: extrakce do `PoseSequence` třídy by byla premature abstraction nad jediným klientem; refactor přijde až s Demo04 jako 2. klient. **Demo04 Stage 1 = Židle**: nová vrstva `src/world/` s `Chair.js` factory. Mesh = sedátko (1.4 × 0.15 × 1.4) + opěrka (1.4 × 1.5 × 0.15) v hnědé. Výška sedátka derivovaná z výšky pelvisu v SIT pose — měření `_measure_sit.html` ukázalo `pelvis.y - floorY = 1.4067 j ≈ H × 0.176` (= výrazně nižší než původní odhad 36 % H). `seatHeightFraction = 0.176`. Approach pos = 1.5 j před židlí. **Stickman.linkTo extension**: `finalYaw` (= cílová orientace po arrival; spustí druhou turn-to fázi pokud diff ≥ 5°), `arrivePosition` (= kam má pelvis přistát; lerp z approach na arrivePosition běží synchronně s post-turn-to yaw lerpem). `_pendingLocomotion` přejmenován na `_pendingAction` (= reuse pro pre-locomotion i post-arrival turn-to dispatch). **Demo04 Stage 2 = Postel**: `Bed.js` factory, matrace 8 × 2.5 × `H×0.176` (= identicky vysoká s vrchem sedátka židle, izomorfismus „úroveň pro sezení/leh") + headboard 1.5 j na +Z konci. `sitEdge` = 0.7 j od dolního okraje matrace, `layCenter` = pivot. Měření `_measure_lay.html` ukázalo bbox postavy v LAY = 7.65 j délka, pelvis on floor. 2-stage interakce řízená externím schedulerem v demu (`bedStep` proměnná, render loop tick): Stage A linkTo k sit edge s `then: SIT` → 0.5 s pauza → Stage B teleport `worldPos = layCenter` + `setStatus(LAY)`. Snap-to-floor override během lying na `floorY + bed.mattressHeight` → postava přistane na vrchu matrace. **Bug objevený**: rotation order Z×Y×X v `Skeleton.computeWorldTransforms` znamená yaw aplikovaný PŘED RX(90°) lay rotace → `worldYaw` při LAY rotuje postavu kolem podélné osy (= roll), ne v rovině world XZ. Workaround pro Stage 2: `bed.facing = 0`. Plný fix přes change rotation order patří do refaktoru kostry s plným auditem (memory `feedback_skeleton_refactor_checklist`). **`demo04_interactables.html`** — sjednocené demo s židlí + postelí + tlačítky (Posaď se, Lehni si, Reset). **GLOSSARY.md** — nový termín „Pose linking (linkTo)". **`src/world/`** = nová vrstva sourozenec character/scene/util/library. Detaily v Sezení 11.

## 2026-04-28

- **Hero GIF z demo09 walk** → `assets/hero-walk.gif` (145 KB, 640×400, 15 fps, ~4 s loop). Aktivní v `README.md` pod tagline. Detaily v `docs/diary/2026-04-28.md` (Sezení 2).
- **GitHub Pages live** → <https://mrklas69.github.io/Stickman/>. Source: Deploy from a branch (`main` / root). Odkaz doplněn do `README.md` pod hero GIF.
- **F1 Inspector hotová** (Sezení 4) — DebugView (5 vrstev), demo01_inspector (poses + slidery + transitions + Copy JSON), GLOSSARY.md, hi-tech paleta, 6 lehových pos. Detaily v Sezení 4.
- **Skeleton změny** (Sezení 4): neck.y twist (DOF 14→15), torso.x záklon -30°, elbow 170°, shoulder.z 120°, UPPER_ARM/FOREARM přerozděleno (0.18 / 0.215).
- **Sjednocení joint = support** (Sezení 4 / F1.5) — supportPoints field deprecated, joints jsou supports automaticky, dynamic contact body. Joint sféry barva PAL.support.
- **F2 prototyp — vrstva `src/character/`** (Sezení 5) — Status enum (10 stavů), Animations registr (STAND/SIT/WALK/LAY hotové), Stickman wrapper s `setStatus/setParams/animate(dt)` + plynulé přechody (Pose.lerp blend, cubic ease-out, default 0.4 s). Demo `demo02_stresstest.html` se 4 tlačítky stavů a slidery WALK parametrů. Detaily v Sezení 5.
- **F1.6 Inspector dotažený** (Sezení 6, dokončeno 2026-04-29) — Support overlay smazán (joint = support visuálně), sekce `POSTOJE`/`SEDY/KLEKY`/`LEHY` se sadami `STANCE_POSES`/`SIT_POSES`/`LIE_POSES`, **8 nových sed/klek pose** (sit přepsán na asymetrický, sitCross, sitLegsForward, sitTuck, sitKneel, sitChild, cat, sitSide) + laySide doladěn. **DOF rozšíření** hipL/R.y + shoulderL/R.y (totalDOF 15→19, axiální rotace stehna a paže = turn-out/turn-in). Limity rozšířené: knee 130→165, hip.z 60→80, torso.z 20→30. **Live slider proporcí** (Skeleton.setProportions + StickmanView.rebuildBones, 6 multiplier sliderů). Random přepíná i rootRotation. Detaily v Sezení 6.

## 2026-04-30

- **F2.3 — Sezení 10: Bio/Fyzio + JUMP + treadmill + Fidget rename + plížení rewrite.** **`src/character/Gestures.js`** (~280 LOC) — overlay diskrétních gest (Bio/Fyzio) s Poisson schedulerem (exp inter-arrival pro inter-event time). Sourozenec Fidget/Breathing v render pipeline. 7 gest implementovaných: `armsAkimbo`, `armsFolded`, `armsBehindHead`, `scratchHead`, `stretch` (V-tvar paží), `weightShiftLeft`, `weightShiftRight`. Keyframes formát: `{ at, angles: { joint: { axis: deg } }, root?: { rx,ry,rz,px,py,pz } }`; root delta je aditivní k primárce (Drift dál točí pelvis, gesto přidá tilt/posun). Lambda slider 0–0.5/s v demo02 + manuální trigger buttons per gesto. Status enum + isResting filtr (= STAND idle / SIT). Abort mid-gesture při změně Status. **`Status.JUMP`** s `animateJump` (5 fází: PREP → PUSH → AIR → LAND → RECOVER s smoothstep blendingem) a 3 presety: `vertical` (lift 1.5, sym), `long/žabák` (lift 0.8 + torsoLean 35° + sym), `running` (tempo 1.5 + scissor nohy 50° + kontralaterální paže). Snap-to-floor skip přes `params._airborne` flag během AIR fáze. **Šachovnicová podlaha** v `BasicScene` (2j dlaždice, 67% opacity, NearestFilter pro ostré hrany). Floor 40×40 → 80×80, shadow bounds ±12 → ±30, mapSize 1024 → 2048. **Treadmill v demo02** — postava centrovaná v origin, podlaha posouvá texture offset opačně k pohybu (texture.repeat = 40, offset = worldPos / 2). Foot tracking algoritmus: každý frame identifikuj všechny supports (joints s Y ≤ minY+tolerance), drž `supportHistory` counter per joint, pick joint s nejdelší history → použij jeho world Z deltu jako body velocity. Pro JUMP fallback na manual `params.forwardSpeed`. Walk/Jog/Sprint/Crawl/Sneak fungují plynule. Plížení selhává — vyžaduje další ladění (viz TODO F2.4). **`forwardSpeed` smazán** z DEFAULTS_WALK/RUN_PRESETS/CRAWL/PRONE/SNEAK (= magic numbers nahrazeny dynamickým výpočtem); JUMP ho ponechává jako manual override. **Rename `src/util/Neklid.js` → `src/util/Fidget.js`** (globální izomorfismus názvů AJ). `applyNeklid` → `applyFidget`, `NEKLID_EXCLUDED*` → `FIDGET_EXCLUDED*`. Doménový pojem „Neklid" zachován v UI labelech a docs prose. Refs aktualizované v Gestures.js, Breathing.js, Animations.js, demo02. **PRONE rewrite** — z Bezier-through-MID na asymetrický 4-fázový cyklus per noha (REACH → PUSH → PASSIVE drag → REACH) podle uživatelské biomechanické reference. Per-leg pronePhase fn s 4 segmenty + smoothstep, diagonální párování L paže s P nohou (a opačně) přes generic `pronePhase(t, KEY_R, KEY_A, KEY_P)`. Nohy v dobrém stavu, paže + spine C-curve + pelvis rotation potřebují další iteraci. **`@THINK` Demo03 (PoseSequence) + Demo04 (Interactable)** — fundament pro pose linking + world objekty (židle, postel, klika). Zapsáno do IDEAS.md jako roadmap pro F3+. Detaily v Sezení 10.

## 2026-04-29

- **F2.2 — Sezení 9: synchronizace + audit + 3 nové statusy + Neklid/Drift přepis.** Globální memory + Stickman PROMPTS.md zrekonstruovány z PocketStory/TheCubes/Voidspan referencí. **`@AUDIT:CODE`** opravil DOF count `19→22` napříč 7 souborů, smazal mrtvé API (`Skeleton.setProportions` + `StickmanView.rebuildBones`, ~80 LOC), shallow-copy `Stickman.params` (F3-akvárium safe), stale komentáře (RUN „let-fáze", DANCE „samostatná animace", `getContactPoints` neexistuje). **`@AUDIT:DOCS`** opravil `Pose ≠ supportPoints` na 4 místech, duplikovaný GLOSSARY `### Stickman` header, stale `STANCE_POSES` obsah, číselné chyby (8 lehových → 6, 14 dem → 11), DONE.md chyběl Sezení 8, „Crash test markery" sekce (= feature který už neexistuje) přepsána na „Body markery". **Smazáno 6 dem** (04, 06, 08, 09, 13, 14) — zbylo 5: 01_inspector, 02_stresstest, 10_ik, 11_swim, 12_climb. **Neklid** přepsán podle user feedbacku „postava vlaje ve větru jako pověšená za pelvis": vrch těla jen (`torso.x/z`, `neck.x/z`, `shoulderL/R.*`, `elbowL/R.*`), žádné `rootRotation`, vyloučené `pelvis/hipL/R/kneeL/R` + twist osy. Memory `feedback_neklid_anatomical_scope.md`. **Drift** přepsán z budget walk na `lerp toward random P2` (parametr `fraction` = jemnost driftu, default 0.25 = „čtvrtinový vektor"; `step` parametr odstraněn). **WALK** kývání horního těla na polovinu (`twistAmp 8→4`, `swayAmp 4→2`). **Dýchání** nový overlay `src/util/Breathing.js` (~0.25 Hz sinusoida na `torso.x + neck.x`, max 3°/1.5° při level 10, biologicky koherentní = jeden phase). **3 nové statusy**: `CRAWL` (po čtyřech, dog-trot, báze cat pose, narovnaná záda, tempo 0.47, hlava nahoru přes neck.x=-50); `PRONE` (plížení, **dvojtaktní kvadratický Bezier** přes user-authored MID jako passing point, hadí undulation `torso.y/z` + counter-twist `neck.y/z`, contralateral live wiggle per končetina; 6 iterací: procedurální → BASE/INVERSE lerp → 4-segment → 3-key → Bezier-through-3, pelvis bobs ~0.7 j během transitions = nezvedání kolen pod podlahu); `SNEAK` (přikrčená/maskovaná chůze, kachní silueta, `hipBase 80` + `kneeBase 110`, pelvis 57% výchozí výšky). **Limit rozšíření**: `neck.x [-30, 60] → [-60, 60]` (anatom. max záklon), `hipL/R.z [.., 80] → [.., 90]` (plížení MID authoring headroom). **`@END`** rozšířen o krok „Permission cleanup" (konsolidace `~/.claude/settings.json` wildcardů). Detaily v Sezení 9.

- **F2.1 — Drift + Run + Neklid + Dance preset** (Sezení 8). **`animateRun` + `RUN_PRESETS`** (jog tempo 1.6 / sprint tempo 2.4) — větší stepAmp, kneeLift, forwardLean, ohnutý loket běžce. Sprint kalibrován: forwardLean 30°, kneeLift 150°. **`animateStand` rozšířen o Drift** (= budget-based random walk, spec z IDEAS.md): lazy state v `params._drift`, quint ease-out per cyklus, root rotace v poolu s `rootRange = 90°` (plný drift). **Dance jako preset Status.STAND idle**, ne separátní animace: `setStatus(STAND, { idle: true, fraction: 0.5, cycleDuration: 60/110, rootRange: 8 })`. **Neklid jako globální overlay** v `src/util/Neklid.js`: sin oscilace per-osa s deterministickou frekvencí (hash z `joint.axis`), level 0–10 → max 5° per kloub / 4° root, aplikuje se PO `Stickman.animate`. **Bug fix** `skeleton.reset()` na začátku `animateWalk/Run` (jinak by úhly z předchozí animace přetrvávaly v ne-přepsaných osách). **`demo02_stresstest.html` přepsán** podle vzoru Demo 01: 3-column layout, levý panel POSES (Reset/Dance/Drift) + STATUS (Walk/Jog/Sprint), pravý panel jen sekce „Globální → Neklid". Detaily v Sezení 8.

- **F1.7 — uzavření iterace 01 (Demo 01)** (Sezení 7). **Refresh proporcí** v `buildProportions`: biceps & předloktí × 0.95 (UPPER_ARM 0.180→0.171, FOREARM 0.215→0.204), stehno & lýtko × 1.05 (THIGH/SHIN 0.245→0.257). Sweet spot zafixován do default po experimentu se sliderem proporcí (slidery následně odstraněny z UI). **6 nových authored postojů**: `bow` (Klanění), `arabesque` (Arabeska), `stretch` (Protažení), `crow` (Vrána), `bridge` (Most), `headstand` (Stoj na hlavě). Pořadí podle yoga progression. STANCE_POSES přepsán: primitivní stand/tpose/wave/squat odstraněny ze sekce (zůstávají v BASIC_POSES). **Cleanup UI:** sekce „Ostatní" odstraněna z Inspectoru, sekce „Proporce" odstraněna, bottom panel s toggles odstraněn (CoM/gravity/body markery vždy ON přes init `setVisible`). **Quint ease-out** (1−(1−t)⁵) místo cubic pro pose transitions. **5 starých dem smazáno**: demo01_static, demo02_poses, demo03_playground, demo05_com, demo07_snap (funkce pohlcené v Inspectoru). README + index.html + GLOSSARY aktualizovány. **Drift algoritmus** zapsán do IDEAS.md jako kandidát na F2 STAND-idle: 3-fázový budget-based random walk s expo/quint ease-out. Detaily v Sezení 7.
